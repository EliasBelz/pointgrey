const Featured: React.FC = () => {


  return (
    <div>
       {/* <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-3 gap-4">
        {["the-boys"].map((production, i) => (
          <div
            key={production.slug}
            ref={(el) => (cardRefs.current[i] = el)}
          >
            <Card {...production} />
          </div>
        ))}
      </div>
      <p>

      </p> */}
    </div>
  );

}

export default Featured;